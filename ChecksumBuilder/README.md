# How-To-Use
Open terminal and verify that you are inside the ChecksumBuilder folder that holds the script. If images are in a subdirectory, such as here, use 
`python make_manifest.py Sample_images`. If images are inside the same folder as the script, simply use `python make_manifest.py`. 